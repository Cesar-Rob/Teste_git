import { StyleSheet, Text, View } from 'react-native';

export default function App() {
  //string
  let nome:string = "Robson";

  //number
  let idade:number = 17;

  //boolena
  let statusMatricula:boolean = true;

  //array
  let pessoas:string[] = ["Rob", "San", "Eli"];
  let idades:number[] = [17, 16 , 11];

  //tuple
  let dadosPessoa:[string,number,string,boolean] = ["César", 17.6, "FMM", true];

  //object
  type paciente ={
    nome:string,
    idade?:number,
    status:boolean
  }


  let paciente:paciente = {
    nome:"Francisco",
    idade:19,
    status:false

  }


  return (
    <View style={styles.container}>
      <Text> Nome: {nome}</Text>
      <Text> idades: {idade}</Text>
      <Text> Status Matrícula: {statusMatricula}</Text>
      <Text> nome selecionado Rob: {pessoas[0]}</Text>
      <Text> idade selecionado: {idades[0]}</Text>
      <Text> Nome paciente: {paciente.nome}</Text>
      <Text> Nome idade: {paciente.idade}</Text>
      <Text>{`status: ${paciente.status}`} </Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#f0f0f0',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
